# ──────────────────────────────────────────────────────────────────────────────
#  Meme Detection Backend — Python Dependencies (api-inference branch)
#  PyTorch + transformers REMOVED — inference delegated to HuggingFace Inference API.
#  Total backend install size: ~50MB (vs ~650MB with PyTorch)
# ──────────────────────────────────────────────────────────────────────────────

# ── Web framework ──────────────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6

# ── Pydantic (v2) ─────────────────────────────────────────────────────────────
pydantic>=2.9.0,<3.0.0
pydantic-settings==2.5.2

# ── Database — SQLAlchemy 2.0 + PostgreSQL driver ─────────────────────────────
sqlalchemy>=2.0.35,<3.0.0
# psycopg3 (psycopg[binary]) — use instead of psycopg2 for Python 3.12+ / 3.14 support.
psycopg[binary]==3.2.10

# ── Alembic migrations ────────────────────────────────────────────────────────
alembic==1.13.2

# ── File uploads (multipart/form-data) ───────────────────────────────────────
python-multipart==0.0.9

# ── Environment variable loading ──────────────────────────────────────────────
python-dotenv==1.0.1

# ── HTTP client — used for HuggingFace Inference API calls ───────────────────
# Replaces the entire torch+transformers stack for inference.
# API call: POST https://api-inference.huggingface.co/models/<model-id>
httpx>=0.27.0,<1.0.0

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.3.2
pytest-asyncio==0.24.0

# ── OCR (Phase 2) ─────────────────────────────────────────────────────────────
Pillow>=10.0.0
pytesseract==0.3.13
